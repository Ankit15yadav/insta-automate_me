import React from "react"

type props = {
    children: React.ReactNode
    type: "FREE" | "PRO"
}

export const SubscriptionPlan = ({ children, type }: props) => {
    // wip return subscription plan of user
    return children
}